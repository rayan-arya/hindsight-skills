# Hindsight skills — environment variables.
# Copy this file to .env and fill in the values. .env is gitignored.

# Anthropic API key — used by all five skills to call Claude.
ANTHROPIC_API_KEY=

# ZeroEntropy API key — brain-page retrieval in calibrated-advise (side quest).
ZEROENTROPY_API_KEY=

# The Hog API key — fresh-signal layer in calibrated-advise (side quest).
THEHOG_API_KEY=
